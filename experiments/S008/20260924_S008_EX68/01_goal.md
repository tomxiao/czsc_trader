# S008 EX68 目标

EX67的REX执行已形成固定receipt，但生成的manifest缺少`symbol`，最终裁决为
`ARCHIVE_VALIDATION_FAILED`。本实验是纯技术后继，只验证能否从EX67持久化证据恢复前序输入，
原样承接其机器事实，并通过平台档案合同形成有效实验。

禁止重新读取EX42、EX48、EX66原始证据，禁止重新计算trial统计、改变决策规则、读取封存验证区、
创建原型、运行搜索或形成候选。
