# S008 EX78 执行

按预注册协议读取并校验 EX68、EX42、EX48 与 EX16 的固定身份。执行至 EX48 差距账本字段选择时，代码请求不存在的 `negative_avoidance_ratio` 列；真实列名为 `negative_log_return_avoidance_ratio`，Pandas 抛出 `KeyError`。REX 未签发 receipt，未生成可用机会地图或可观测性结果。临时工作空间中的部分中间文件不构成研究证据。
